
 /*class ScrollingBackground {
  constructor(x, y) {
  this.x = x;
  this.y = y;
  const img = new Image();
  img.addEventListener('load', () => {});
  img.src = '../docs/assets/images/AquaMarine';
  this.img = img;*/
  
  
  
    /*  this.position = {
      x: x,
      y: y,
      image: image,
    };
 */
   /*  this.image = image;
    this.width = canvas.width;
    this.height = canvas.height;
    this.image = createImage(backgroundImg); */
/*  }

  drawBackground() {
    ctx.drawImage(this.img, this.x, this.y, cWidth, cHeight);
  }
}; */